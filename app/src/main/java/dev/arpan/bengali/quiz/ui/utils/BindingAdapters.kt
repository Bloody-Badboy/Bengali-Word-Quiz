package dev.arpan.bengali.quiz.ui.utils

/*
@BindingAdapter("options")
fun RecyclerView.bindQuestionOptions(list: MutableList<QuestionItem.Option>?) {
    val optionAdapter: QuestionOptionsAdapter =
        (adapter as? QuestionOptionsAdapter?) ?: QuestionOptionsAdapter().also {
            layoutManager = LinearLayoutManager(context)
            adapter = it
        }
    list?.let {
        optionAdapter.data = list
    }
}*/
